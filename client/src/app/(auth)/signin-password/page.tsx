import { PasswordForm } from "@/forms/auth/password-form";
import { GalleryVerticalEnd } from "lucide-react";
import React from "react";

const page = () => {
  return <PasswordForm />;
};

export default page;
